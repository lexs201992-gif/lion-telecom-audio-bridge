System Ui is the baseline of all the Desing as the standards is ASOP 
but if we analize the virtual invokes and private invokes
at the smali byte code inside the package, we can understand the 
real control by the odm
